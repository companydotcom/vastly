name: Delete submodule branch on closed pr
on:
  push:
    # types: [closed]
    branches:
      - ATX-2519

jobs:
  delete-branch:
    runs-on: ubuntu-latest

    steps:
      - name: delete branch
        run: |
          console.log(context)
        # uses: SvanBoxel/delete-merged-branch@main
        # env:
        #   GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
