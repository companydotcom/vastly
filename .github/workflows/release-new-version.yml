name: Release New Package Version

on:
  push:
    branches:
      - "**"
#
concurrency: "${{ github.workflow }}-${{ github.ref_name }}"

jobs:
  build:
    runs-on: ubuntu-latest
    if: endsWith(${{ github.ref_name }}, 'pkgs')
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Setup PNPM
        uses: pnpm/action-setup@v2
        with:
          version: 7
      - name: Setup Node v16.x
        uses: actions/setup-node@v3
        with:
          node-version: 16.x
          cache: "pnpm"

      - run: pnpm install --frozen-lockfile
      - name: Create Release Pull Request
        id: changesets
        uses: changesets/action@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

          # test npm registry
          # figure out how to switch target branches before PR
          # what is the slack-bot notification
          # running on every pull_request bad
