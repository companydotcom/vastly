import { FormControl, FormLabel, Skeleton, FormHelperText } from "@chakra-ui/react"

export const InputField = () => {
  return (
    <FormControl>
      <FormLabel htmlFor="email">Email address</FormLabel>
    </FormControl>
  )
}
