const dxpConfig = {
  /**
   *  What are the configs we need?
   * @dxp/core is a package that is a dependency of the monorepo template, referenced from the package.json into this config file
   * Its purpose is to control certain functionality of the monorepo template by us. What is the controller??
   * Leaning towards constraining the creator to only being able to populate specific templates into specific directories
   *
   */
};

module.exports = dxpConfig;
